/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ['./src/**/*.{js,jsx,ts,tsx}'],
  theme: {
    extend: {
      // You can add static colors here if needed, but do not use CSS variables
    },
  },
  plugins: [],
};
